apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pso-analytics-gui.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "pso-analytics-gui.name" . }}
    helm.sh/chart: {{ include "pso-analytics-gui.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "pso-analytics-gui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
